export interface HeroSlide {
  id: number;
  image: string;
  title: string;
  description: string;
}

export interface Room {
  id: number;
  title: string;
  price: string;
  image: string;
}

export interface Feature {
  id: number;
  icon: string;
  title: string;
  description: string;
}